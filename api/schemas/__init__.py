"""Pydantic request/response models -- the single source of truth for what
API clients (frontend, dashboard, tests) can rely on (project_refactor.md
§8.2). No route handler returns an ad hoc dict."""
